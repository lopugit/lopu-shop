document.addEventListener(
  'DOMContentLoaded',
  function() {
    const checkoutData = JSON.parse(document.querySelector('#checkoutData').innerHTML)

    // document.addEventListener(
    //   'page:change',
    //   function() {
    //   },
    //   false
    // )

    // Render checkout changes on first load

    // if (Shopify.Checkout.step === 'contact_information') {
    // } else if (Shopify.Checkout.step === 'shipping_method') {
    // } else {
    // }
  },
  false
)
